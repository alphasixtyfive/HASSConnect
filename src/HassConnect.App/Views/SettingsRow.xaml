<UserControl x:Class="HassConnect.App.Views.SettingsRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="Row" MinHeight="52" Padding="16,0" SizeChanged="Row_SizeChanged"
          BorderBrush="{ThemeResource SectionBorderBrush}" BorderThickness="0,0,0,1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <FontIcon x:Name="RowIcon" Grid.Column="0" FontSize="18" Width="20"
                  Margin="0,0,16,0" VerticalAlignment="Center" Visibility="Collapsed"
                  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <Grid Grid.Column="1" VerticalAlignment="Center" Margin="0,0,16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Style="{StaticResource BodyTextBlockStyle}"
                       VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,8,16,8" />
            <TextBlock x:Name="ValueLabel" Grid.Column="1" Style="{StaticResource CaptionTextBlockStyle}"
                       VerticalAlignment="Center" MaxWidth="180"
                       TextTrimming="CharacterEllipsis" Visibility="Collapsed"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>
        <ContentPresenter x:Name="TrailingPresenter" Grid.Column="2"
                          HorizontalAlignment="Right" VerticalAlignment="Center">
            <ToggleSwitch x:Name="Switch" Width="44" Height="44" MinWidth="0"
                          OnContent="" OffContent="" VerticalAlignment="Center"
                          Toggled="Switch_Toggled" />
        </ContentPresenter>
    </Grid>
</UserControl>
